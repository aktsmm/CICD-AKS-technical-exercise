name: 3. 🧹 Cleanup Workflow Runs (Keep latest 7 human + 3 dependabot runs)

on:
  # ✅ 定期実行（30分ごと、UTC基準）
  schedule:
    - cron: "*/30 * * * *"

  # ✅ 手動実行
  workflow_dispatch:

  # ✅ 他ワークフローが完了したとき（成功・失敗どちらでも）
  workflow_run:
    workflows:
      - "Deploy Infrastructure"
      - "Build and Deploy Application"
      - "Deploy Policy Guardrails"
    types:
      - completed

permissions:
  actions: write
  contents: read

jobs:
  cleanup:
    # ✅ mainブランチでのみ実行
    if: |
      (
        github.ref == 'refs/heads/main' ||
        (
          github.event_name == 'workflow_run' &&
          github.event.workflow_run.head_branch == 'main'
        )
      ) &&
      (
        github.event_name == 'schedule' ||
        github.event_name == 'workflow_dispatch' ||
        (
          github.event_name == 'workflow_run' &&
          (
            github.event.workflow_run.conclusion == 'failure' ||
            github.event.workflow_run.conclusion == 'success'
          )
        )
      )

    runs-on: ubuntu-latest

    steps:
      - name: 🪶 Debug trigger info
        run: |
          echo "Event: ${{ github.event_name }}"
          echo "Workflow Run Conclusion: ${{ github.event.workflow_run.conclusion }}"
          echo "Head Branch: ${{ github.event.workflow_run.head_branch }}"

      - name: 🧹 Cleanup old workflow runs (keep latest 7 human + 3 dependabot + current cleanup)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          REPO="${{ github.repository }}"
          SELF_NAME="🧹 Cleanup Workflow Runs (Keep latest 7 human + 3 dependabot runs)"

          echo "🧾 Fetching workflow runs..."
          RUNS=$(gh api repos/$REPO/actions/runs --paginate \
            -q '.workflow_runs[] | {id: .id, name: .name, status: .status, conclusion: .conclusion, actor: .actor.login, created_at: .created_at}' \
            | jq -s 'sort_by(.created_at) | reverse')

          echo "🧩 Selecting deletion targets..."

          # ✅ 最新のCleanup Run（＝今回分）を取得して保持
          CURRENT_RUN_ID=$(gh run list --workflow "$SELF_NAME" --json databaseId,status \
            | jq -r '.[] | s
