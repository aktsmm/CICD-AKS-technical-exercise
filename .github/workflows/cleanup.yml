name: 🧹 Cleanup Workflow Runs (Keep latest 7 human + 3 dependabot runs)

on:
  # ✅ 定期実行（30分ごと、UTC基準）
  schedule:
    - cron: "*/30 * * * *"

  # ✅ 手動実行（必要に応じて）
  workflow_dispatch:

  # ✅ 他ワークフローが「失敗」したときに実行
  workflow_run:
    workflows:
      - "Deploy Infrastructure"
      - "Build and Deploy Application"
      - "Deploy Policy Guardrails"
    types:
      - completed

permissions:
  actions: write
  contents: read

jobs:
  cleanup:
    # 🔒 mainブランチかつ Cleanup 自身のワークフロー以外で実行時だ！
    if: |
      (
        github.ref == 'refs/heads/main' ||
        (
          github.event_name == 'workflow_run' &&
          github.event.workflow_run.head_branch == 'main'
        )
      ) &&
      github.workflow != '🧹 Cleanup Workflow Runs (Keep latest 7 human + 3 dependabot runs)' &&
      (
        github.event_name == 'schedule' ||
        github.event_name == 'workflow_dispatch' ||
        (
          github.event_name == 'workflow_run' &&
          (
            github.event.workflow_run.conclusion == 'failure' ||
            github.event.workflow_run.conclusion == 'success'
          )
        )
      )

    runs-on: ubuntu-latest

    steps:
      - name: Delete failed, old, and all Cleanup runs (keep latest 7 human + 3 dependabot)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          REPO="${{ github.repository }}"
          SELF_NAME="🧹 Cleanup Workflow Runs (Keep latest 7 human + 3 dependabot runs)"

          echo "🧾 Fetching workflow runs..."
          RUNS=$(gh api repos/$REPO/actions/runs --paginate \
            -q '.workflow_runs[] | {id: .id, name: .name, status: .status, conclusion: .conclusion, actor: .actor.login, created_at: .created_at}' \
            | jq -s 'sort_by(.created_at) | reverse')

          echo "🧩 Selecting deletion targets..."

          # ✅ 人間実行: 最新7件のみ保持
          KEEP_HUMAN=$(echo "$RUNS" | jq -c '[.[] | select(.actor != "dependabot[bot]" and .conclusion == "success")][:7] | map(.id)')

          # 🤖 Dependabot実行: 最新3件のみ保持
          KEEP_DEPENDABOT=$(echo "$RUNS" | jq -c '[.[] | select(.actor == "dependabot[bot]" and .conclusion == "success")][:3] | map(.id)')

          # 🧩 保持対象を統合
          KEEP_ALL=$(jq -n --argjson a "$KEEP_HUMAN" --argjson b "$KEEP_DEPENDABOT" '$a + $b')

          TARGETS=$(echo "$RUNS" | jq -c --arg self "$SELF_NAME" --argjson keepIds "$KEEP_ALL" '
            [ .[]
              | select(
                  (.name == $self)                           # ✅ Cleanup自体は常に削除
                  or (.conclusion != "success")              # ❌ 失敗/キャンセルRun削除
                  or (.id as $id | ($keepIds | index($id)) | not)  # ❌ 保持リスト外を削除
                )
            ]')

          COUNT=$(echo "$TARGETS" | jq 'length')
          echo "🧹 Found $COUNT runs to delete."

          if (( COUNT == 0 )); then
            echo "✅ No runs to delete. Exiting."
            exit 0
          fi

          echo "$TARGETS" | jq -c '.[]' | while read -r run; do
            ID=$(echo "$run" | jq -r '.id')
            NAME=$(echo "$run" | jq -r '.name')
            ACTOR=$(echo "$run" | jq -r '.actor')
            STATUS=$(echo "$run" | jq -r '.status')
            CONCLUSION=$(echo "$run" | jq -r '.conclusion')
            CREATED_AT=$(echo "$run" | jq -r '.created_at')

            echo "🧹 Deleting run ID: $ID [$NAME] by $ACTOR ($STATUS/$CONCLUSION, created $CREATED_AT)"
            gh api repos/$REPO/actions/runs/$ID -X DELETE || echo "⚠️ Failed to delete run $ID"
          done

          echo "🎉 Cleanup complete (7 human + 3 dependabot runs retained)."
