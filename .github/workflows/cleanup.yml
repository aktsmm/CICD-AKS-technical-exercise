name: 🧹 Cleanup Workflow Runs (Keep latest 10 successful runs only)

on:
  push:
    branches:
      - main
    paths:
      - "app/**"
      - "infra/**"
      - ".github/workflows/cleanup.yml"

  workflow_dispatch:
  schedule:
    - cron: "0 */12 * * *" # 12時間ごとに実行（UTC）

permissions:
  actions: write
  contents: read

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Delete failed and old workflow runs (keep latest 10 successful)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          REPO="aktsmm/CICD-AKS-technical-exercise"
          SELF_NAME="🧹 Cleanup Workflow Runs (Keep latest 10 successful runs only)"

          echo "🧾 Fetching workflow runs..."
          RUNS=$(gh api repos/$REPO/actions/runs --paginate \
            -q '.workflow_runs[] | {id: .id, name: .name, status: .status, conclusion: .conclusion, created_at: .created_at}' \
            | jq -s 'sort_by(.created_at) | reverse')

          echo "🧩 Selecting deletion targets..."
          # 直近10件の「成功run」を保持、それ以外を削除
          TARGETS=$(echo "$RUNS" | jq -c --arg self "$SELF_NAME" '
            [ 
              .[10:][]                                         # 11件目以降
              | select(
                  (.conclusion != "success")                   # 成功以外は削除
                  or (.name == $self)                          # Cleanup自身も削除
                )
            ] +
            [
              .[0:10][]                                       # 最新10件内でも
              | select(
                  (.conclusion != "success")                   # 成功以外は削除
                  or (.name == $self)
                )
            ]')

          COUNT=$(echo "$TARGETS" | jq 'length')
          echo "🧹 Found $COUNT runs to delete."

          if (( COUNT == 0 )); then
            echo "✅ No runs to delete. Exiting."
            exit 0
          fi

          echo "$TARGETS" | jq -c '.[]' | while read -r run; do
            ID=$(echo "$run" | jq -r '.id')
            NAME=$(echo "$run" | jq -r '.name')
            STATUS=$(echo "$run" | jq -r '.status')
            CONCLUSION=$(echo "$run" | jq -r '.conclusion')
            CREATED_AT=$(echo "$run" | jq -r '.created_at')

            echo "🧹 Deleting run ID: $ID [$NAME] ($STATUS/$CONCLUSION, created $CREATED_AT)"
            gh api repos/$REPO/actions/runs/$ID -X DELETE || echo "⚠️ Failed to delete run $ID"
          done
