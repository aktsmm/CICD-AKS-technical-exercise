name: 3. 🧹 Cleanup Workflow Runs (Keep latest 7 human + 3 dependabot runs)

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:
  workflow_run:
    workflows:
      - "1. Deploy Infrastructure"
      - "2-1. Build and Deploy Application"
      - "2-2. Deploy Policy Guardrails"
    types: [completed]
  push:
    branches: [main]
    paths:
      - ".github/workflows/cleanup.yml"
      - ".github/workflows/🧹*.yml"

permissions:
  actions: write
  contents: read

jobs:
  cleanup:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 🧹 Cleanup all old workflow runs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          REPO="${{ github.repository }}"
          SELF_NAME="🧹 Cleanup Workflow Runs (Keep latest 7 human + 3 dependabot runs)"

          echo "🔍 Getting all Cleanup workflow runs..."
          RUNS=$(gh run list --workflow "$SELF_NAME" --json databaseId,headBranch,status,conclusion,createdAt --limit 1000)
          COUNT=$(echo "$RUNS" | jq 'length')
          echo "🧾 Found $COUNT Cleanup runs total."

          # 現在の Cleanup 実行中の ID を取得
          CURRENT_ID=$(echo "$RUNS" | jq -r '.[] | select(.status=="in_progress" or .status=="queued") | .databaseId' | head -n 1)
          echo "🟢 Current Cleanup Run ID: ${CURRENT_ID:-none}"

          # 現行Run以外を削除対象とする
          TARGETS=$(echo "$RUNS" | jq -r --arg cur "$CURRENT_ID" '.[] | select(.databaseId != ($cur|tonumber)) | .databaseId')

          if [ -z "$TARGETS" ]; then
            echo "✅ No old Cleanup runs to delete."
          else
            echo "$TARGETS" | while read -r ID; do
              echo "🧹 Deleting old Cleanup run ID: $ID"
              gh api repos/$REPO/actions/runs/$ID -X DELETE || echo "⚠️ Failed to delete run $ID"
            done
          fi

          echo "🧩 Cleaning other workflows (7 human + 3 dependabot)..."
          ALL_RUNS=$(gh api repos/$REPO/actions/runs --paginate -q '.workflow_runs[] | {id: .id, name: .name, conclusion: .conclusion, created_at: .created_at, actor: (.actor.login // "unknown")}' | jq -s 'sort_by(.created_at) | reverse')

          KEEP_HUMAN=$(echo "$ALL_RUNS" | jq -c '[.[] | select(.actor != "dependabot[bot]" and .conclusion == "success")][:7] | map(.id)')
          KEEP_DEPENDABOT=$(echo "$ALL_RUNS" | jq -c '[.[] | select(.actor == "dependabot[bot]" and .conclusion == "success")][:3] | map(.id)')
          KEEP_ALL=$(jq -n --argjson a "$KEEP_HUMAN" --argjson b "$KEEP_DEPENDABOT" '$a + $b')

          TARGETS=$(echo "$ALL_RUNS" | jq -c --argjson keepIds "$KEEP_ALL" '
            [ .[] | select((.conclusion != "success") or (.id as $id | ($keepIds | index($id)) | not)) ]')

          COUNT=$(echo "$TARGETS" | jq 'length')
          echo "🧹 Found $COUNT other workflow runs to delete."

          if (( COUNT > 0 )); then
            echo "$TARGETS" | jq -c '.[]' | while read -r run; do
              ID=$(echo "$run" | jq -r '.id')
              NAME=$(echo "$run" | jq -r '.name')
              echo "🗑️ Deleting run $ID [$NAME]"
              gh api repos/$REPO/actions/runs/$ID -X DELETE || echo "⚠️ Failed to delete run $ID"
            done
          fi

          echo "🎉 Cleanup complete (kept 1 current + 7 human + 3 dependabot)."
