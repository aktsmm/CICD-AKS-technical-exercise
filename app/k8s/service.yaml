apiVersion: v1
kind: Service
metadata:
  name: guestbook-service
  namespace: default
spec:
  type: ClusterIP  # Ingress経由でアクセス
  selector:
    app: guestbook
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
