<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BBS - Azure Kubernetes Service</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        font-family: "MS PGothic", "Osaka-Mono", monospace, sans-serif;
        background: #e0e0e0;
        padding: 10px;
      }
      .container {
        max-width: 900px;
        margin: 0 auto;
        background: #fff;
        border: 3px double #666;
      }
      .header {
        background: #336699;
        color: #fff;
        padding: 15px;
        border-bottom: 3px solid #224466;
      }
      .header h1 {
        font-size: 1.5em;
        font-weight: bold;
        letter-spacing: 2px;
        margin-bottom: 5px;
      }
      .header-info {
        font-size: 0.85em;
        opacity: 0.9;
        display: flex;
        gap: 15px;
        align-items: center;
        margin-top: 8px;
      }
      .badge {
        background: #ff6b6b;
        color: #fff;
        padding: 2px 8px;
        font-size: 0.75em;
        border: 1px solid #cc0000;
      }
      .secret-link {
        color: #ffff00;
        text-decoration: underline;
        font-size: 0.85em;
      }
      .secret-link:hover {
        color: #ffdd00;
      }
      .stats {
        background: #f5f5f5;
        padding: 8px 15px;
        border-bottom: 1px solid #ccc;
        font-size: 0.9em;
      }
      .form-section {
        padding: 20px;
        background: #fafafa;
        border-bottom: 2px solid #ddd;
      }
      .form-section h2 {
        font-size: 1.2em;
        margin-bottom: 15px;
        color: #333;
        border-left: 4px solid #336699;
        padding-left: 10px;
      }
      table.form-table {
        width: 100%;
        border-collapse: collapse;
      }
      table.form-table td {
        padding: 8px;
        vertical-align: top;
      }
      table.form-table td:first-child {
        width: 100px;
        font-weight: bold;
        color: #333;
      }
      .form-input,
      .form-textarea {
        width: 100%;
        padding: 6px;
        border: 1px solid #999;
        font-family: inherit;
        font-size: 0.95em;
      }
      .form-textarea {
        min-height: 80px;
        resize: vertical;
      }
      .btn-submit {
        background: #336699;
        color: #fff;
        border: 2px outset #336699;
        padding: 8px 25px;
        font-size: 1em;
        cursor: pointer;
        font-weight: bold;
      }
      .btn-submit:hover {
        background: #2a5580;
      }
      .btn-submit:active {
        border-style: inset;
      }
      .messages-section {
        padding: 20px;
      }
      .messages-section h2 {
        font-size: 1.2em;
        margin-bottom: 15px;
        color: #333;
        border-left: 4px solid #336699;
        padding-left: 10px;
      }
      table.message-table {
        width: 100%;
        border-collapse: collapse;
        border: 1px solid #999;
      }
      table.message-table th {
        background: #336699;
        color: #fff;
        padding: 8px;
        text-align: left;
        font-weight: bold;
        border: 1px solid #666;
      }
      table.message-table td {
        padding: 10px;
        border: 1px solid #ccc;
        vertical-align: top;
      }
      table.message-table tr:nth-child(even) {
        background: #f9f9f9;
      }
      table.message-table tr:hover {
        background: #ffffcc;
      }
      .msg-author {
        font-weight: bold;
        color: #336699;
      }
      .msg-date {
        color: #666;
        font-size: 0.85em;
        margin-left: 10px;
      }
      .msg-content {
        margin-top: 8px;
        line-height: 1.5;
        white-space: pre-wrap;
        word-wrap: break-word;
      }
      .no-messages {
        text-align: center;
        padding: 40px;
        color: #666;
        font-style: italic;
      }
      .footer {
        background: #f5f5f5;
        padding: 10px;
        text-align: center;
        font-size: 0.8em;
        color: #666;
        border-top: 1px solid #ccc;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>■ BBS - Azure Kubernetes Service ■</h1>
        <div class="header-info">
          <span>AKS Demo Environment</span>
          <span class="badge">VULNERABLE</span>
          <a href="/wizexercise.txt" class="secret-link">[Secret File]</a>
        </div>
      </div>

      <div class="stats">■ Total Posts: <%= messages.length %> | Online: 1</div>

      <div class="form-section">
        <h2>■ 新規投稿</h2>
        <form action="/post" method="POST">
          <table class="form-table">
            <tr>
              <td>名前:</td>
              <td>
                <input
                  type="text"
                  name="name"
                  class="form-input"
                  required
                  placeholder="Your Name"
                />
              </td>
            </tr>
            <tr>
              <td>メッセージ:</td>
              <td>
                <textarea
                  name="message"
                  class="form-textarea"
                  required
                  placeholder="Enter your message..."
                ></textarea>
              </td>
            </tr>
            <tr>
              <td></td>
              <td>
                <button type="submit" class="btn-submit">[ 投稿 ]</button>
              </td>
            </tr>
          </table>
        </form>
      </div>

      <div class="messages-section">
        <h2>■ 投稿一覧</h2>
        <% if (messages.length === 0) { %>
        <div class="no-messages">まだ投稿がありません</div>
        <% } else { %>
        <table class="message-table">
          <thead>
            <tr>
              <th style="width: 25%">投稿者</th>
              <th>メッセージ</th>
            </tr>
          </thead>
          <tbody>
            <% messages.forEach(msg => { %>
            <tr>
              <td>
                <span class="msg-author"><%= msg.name %></span>
                <div class="msg-date">
                  <%= new Date(msg.createdAt).toLocaleString('ja-JP') %>
                </div>
              </td>
              <td>
                <div class="msg-content"><%= msg.message %></div>
              </td>
            </tr>
            <% }); %>
          </tbody>
        </table>
        <% } %>
      </div>

      <div class="footer">Powered by Node.js + MongoDB on Azure AKS</div>
    </div>
  </body>
</html>
